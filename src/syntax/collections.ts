export type {
  ArrayType as $Array,
  TupleType as $Tuple,
  NamedTupleType as $NamedTuple,
} from "../reflection";

export {
  ArrayType as array,
  TupleType as tuple,
  NamedTupleType as namedTuple,
} from "../reflection";
